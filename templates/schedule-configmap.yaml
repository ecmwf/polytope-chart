{{- if .Values.schedule }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: schedule
  labels:
    {{- include "polytope-server.labels" . | nindent 4 }}
data:
  schedule.xml: |
    {{- .Values.schedule | nindent 4 }}
{{- end }}
