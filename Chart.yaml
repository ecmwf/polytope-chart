apiVersion: v2
name: polytope-server
description: >
  Polytope data retrieval server backed by the BITS routing engine.
  Deploys the polytope-server frontend (broker), dynamic worker pools,
  and NATS JetStream persistence.
type: application
version: 2.0.1
appVersion: "2.0.0"

dependencies:
  - name: nats
    version: 2.12.4
    repository: https://nats-io.github.io/k8s/helm/charts/
    condition: nats.enabled
  - name: bobs
    version: 0.1.1
    repository: file://../bobs/chart
    condition: bobs.enabled
  # Auth-o-tron 0.4.0 is the target RS256 dependency. Keep the reproducible
  #  0.3.0 lock until eccr.ecmwf.int/auth-o-tron/auth-o-tron-chart:0.4.0 is
  #  published, then update this version and regenerate Chart.lock together.
  - name: auth-o-tron-chart
    version: 0.3.0
    repository: oci://eccr.ecmwf.int/auth-o-tron
    condition: auth-o-tron.enabled
    alias: auth-o-tron
